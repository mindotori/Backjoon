import java.util.Scanner;

public class CountNum {
    public static void main(String[] args) {
        //공백이 연속해서 나오는 경우는 없다. 문자열은 공백으로 시작하거나 끝날 수 있다.*****
        Scanner sc =  new Scanner(System.in);
        String s = sc.nextLine().trim();
        if(!trim)
    }
}
